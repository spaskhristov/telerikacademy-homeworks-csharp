11. Describe the difference between C# and .NET Framework.

Answer:
C# is a programming language, .NET is an application framework. 
The .NET libraries can run on the Common Language Runtime and thus
 any language which can run on the CLR can also use the .NET libraries.